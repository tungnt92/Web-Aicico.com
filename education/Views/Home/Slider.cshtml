@model IEnumerable<education.Models.mediagallery>
<ul class="slides">
    @foreach (var item in Model)
    {
        <li data-thumb="assets/img/slider/@item.filename">
            <img src="assets/img/slider/@item.filename">
            <div class="flex-caption">
                @item.caption
            </div>
        </li>
    }
</ul>